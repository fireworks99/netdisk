<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.netdisk.mapper.SysUserMapper">

    <!-- 用户基础映射 -->
    <resultMap id="SysUserResultMap" type="com.example.netdisk.entity.SysUser">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="nickname" column="nickname"/>
        <result property="status" column="status"/>
        <result column="create_time"  property="createTime"/>
        <result column="update_time"  property="updateTime"/>
    </resultMap>

    <!-- 根据用户名查询 -->
    <select id="findUserByUsername" resultMap="SysUserResultMap">
        SELECT id, username, password, nickname, status, create_time, update_time
        FROM sys_user
        WHERE username = #{username}
        LIMIT 1
    </select>

    <!-- 查询用户角色 -->
    <select id="findRolesByUserId" resultType="string">
        SELECT r.role_code
        FROM sys_role r
                 INNER JOIN sys_user_role ur ON ur.role_id = r.id
        WHERE ur.user_id = #{userId}
    </select>

    <!-- 查询用户权限 -->
    <select id="findPermsByUserId" resultType="string">
        SELECT p.permission_code
        FROM sys_permission p
                 INNER JOIN sys_role_permission rp ON rp.permission_id = p.id
                 INNER JOIN sys_user_role ur ON ur.role_id = rp.role_id
        WHERE ur.user_id = #{userId}
    </select>

    <!-- 新增用户 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_user (username, password, nickname, status, create_time, update_time)
        VALUES (#{username}, #{password}, #{nickname}, #{status}, now(), now())
    </insert>

    <!-- 删除用户 -->
    <delete id="deleteById">
        DELETE FROM sys_user WHERE id = #{userId}
    </delete>

    <select id="findById" resultMap="SysUserResultMap">
        select * from sys_user
        where id = #{userId}
    </select>

    <select id="findAll" resultMap="SysUserResultMap">
        select * from sys_user
    </select>

</mapper>